# PostgreSQL Database configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-d8vbmd0js32c738toibg-a.oregon-postgres.render.com:5432/origindata}
spring.datasource.username=${DB_USERNAME:origindata_user}
spring.datasource.password=${DB_PASSWORD:1vSd1CwiIJeOCDaKNCgt3XgHLhl8PJ04}
spring.datasource.driverClassName=org.postgresql.Driver

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# JWT Secret and Expiration
hofgen.app.jwtSecret=${JWT_SECRET:hofgenSecretKeyCarbonChain2026AdvancedAgenticCodingSecurityKey}
hofgen.app.jwtExpirationMs=86400000

# Server port
server.port=${PORT:8080}

# CORS - comma-separated allowed origins (override via env variable on Render)
cors.allowedOrigins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000,https://origin-carbon-hof1.onrender.com}

# Logging
logging.level.org.springframework.security=DEBUG
